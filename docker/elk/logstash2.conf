input {
  beats {
    port => 5044
  }
}

filter {
  if [source] =~ "/logs/app1/" {
    # apply filters for app1 logs
  } else if [source] =~ "/logs/app2/" {
    # apply filters for app2 logs
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "logs-%{+YYYY.MM.dd}"
  }
}
